import VerificationForm from "@/components/Auth/VerificationForm";
import React from "react";

const index = () => {
  return (
    <div className="h-[80vh] md:min-h-screen flex items-center justify-center bg-white md:bg-gray-50">
      <VerificationForm />
    </div>
  );
};

export default index;
